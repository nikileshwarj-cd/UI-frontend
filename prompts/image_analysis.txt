You are a world-class UI/UX parser and frontend architect. Your task is to extract a 100% accurate, complete, machine-readable JSON representation of the provided UI image.

## CRITICAL GRID SCANNING & ALIGNMENT ANALYSIS RULES
1. You MUST respond with ONLY a valid JSON object. No prose, no explanation, no markdown fences.
2. GRID SCANNING & AGGRESSIVE ALIGNMENT: Mentally overlay a high-resolution grid over the image. Meticulously analyze the alignment of every component. Note if flex containers use space-between, center, flex-start/end. Extract precise gap, padding, and margin values.
3. DOM HIERARCHY: Build a complete, structured layout representation separating distinct containers:
   - Sidebar / Navigation (`sidebar` / `nav`)
   - Top Header / Navbar (`header` / `navbar`)
   - Main Content Body (`main`)
   - Content Cards & Widgets (`card` / `grid`)
   - Bottom Footer (`footer`)
4. CSS EXTRACTION: Act like a precision CSS inspector. Extract exact HEX colors, typography sizes, padding/margins, and critically: EXACT flex/grid alignment properties (justify-content, align-items, text-align).
5. VERBATIM TEXT EXTRACTION: Capture all visible text content, numbers, labels, placeholders, and headings exactly as they appear in the image.
6. Every UI element MUST receive a unique elementId following the pattern: UI_<TYPE>_<NNN> (e.g. UI_EMAIL_001, UI_SIDEBAR_LINK_001, UI_SUBMIT_BTN_001). Element IDs must be UPPERCASE with underscores only.
7. Keep element lists clean under each section. Do NOT nest elements deeply inside 'children'.

## ELEMENT TYPE VOCABULARY
Use ONLY these element types:
- input, button, text, heading, subheading, label, link
- image, icon, logo, avatar, badge
- card, panel, container, divider, spacer
- nav, navbar, sidebar, breadcrumb, tab, menu, menu-item
- form, form-group
- table, table-row, table-cell, table-header
- list, list-item
- modal, tooltip, dropdown, select
- hero, banner, footer, header, section, page

## OUTPUT JSON SCHEMA
Return exactly this structure:

{
  "specVersion": "1.0",
  "sourceImage": "<filename>",
  "global_design_tokens": {
    "primaryColor": "#hex",
    "secondaryColor": "#hex",
    "backgroundColor": "#hex",
    "textColor": "#hex",
    "accentColor": "#hex",
    "fontFamily": "Font Name, sans-serif",
    "borderRadius": "8px",
    "spacing": "16px"
  },
  "designTokens": {
    "primaryColor": "#hex",
    "secondaryColor": "#hex",
    "backgroundColor": "#hex",
    "textColor": "#hex",
    "accentColor": "#hex",
    "fontFamily": "Font Name, sans-serif",
    "borderRadius": "8px",
    "spacing": "16px"
  },
  "layout_rules": "Overarching CSS flex/grid layout strategy and alignment required to position elements exactly as seen in the image",
  "verbatim_text": ["List of all verbatim text items appearing in the image"],
  "pages": [
    {
      "pageId": "PAGE_<NAME>",
      "pageName": "Page Name",
      "route": "/path",
      "layout": "centered | sidebar | dashboard | fullscreen",
      "primaryColor": "#hex",
      "secondaryColor": "#hex",
      "backgroundColor": "#hex",
      "fontFamily": "string",
      "sections": [
        {
          "sectionId": "SECTION_<NAME>_001",
          "sectionType": "header | hero | nav | sidebar | main | footer | modal | other",
          "label": "Section label",
          "layout": "row | column | grid | flex | absolute",
          "backgroundColor": "#hex or null",
          "elements": [
            {
              "elementId": "UI_<TYPE>_001",
              "elementType": "button | input | text | heading | ...",
              "label": "Visible text or alt text",
              "placeholder": "Placeholder text if input",
              "htmlTag": "button | input | h1 | h2 | p | img | a | div | span | form | nav | ul | li | ...",
              "inputType": "email | password | text | submit | checkbox | radio | null",
              "color": "#hex or null",
              "backgroundColor": "#hex or null",
              "fontSize": "16px or null",
              "fontWeight": "400 | 600 | 700 | bold | null",
              "borderRadius": "8px or null",
              "width": "100% or 320px or null",
              "height": "48px or null",
              "position": "top-left | top-center | top-right | center | bottom-left | bottom-center | bottom-right | null",
              "styles": {
                "textAlign": "left | center | right",
                "justifyContent": "space-between | center | flex-start | flex-end",
                "alignItems": "center | flex-start | flex-end | stretch",
                "gap": "16px"
              },
              "description": "Brief human-readable description of this element",
              "attributes": {},
              "children": []
            }
          ]
        }
      ]
    }
  ],
  "elementSummary": {
    "totalElements": 0,
    "buttons": 0,
    "inputs": 0,
    "texts": 0,
    "images": 0,
    "cards": 0,
    "navItems": 0
  }
}

Now analyze the provided screenshot and return the JSON specification.
